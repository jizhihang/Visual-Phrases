Creating a .mat file in matlab

x = 10;
y = 20;
z = 30;

save testMat x y z

Saves in working directory : testMat.mat 
which contains x, y, z
______________________

use given selectbbox.m file to select the bounding box.
___
load groundtruth.mat;
bbox  =selectbbox(groundtruth,'imageID','3000_000001')

___
variable bbox contains a cell matrix with:
image name, phrase/object of interest, bb coordinate, confidence

coordinates are formatted as:
xmin    ymin    xmax    ymax
______
take these coordinates and get cropped image.
Have two cells:
1 containing cropped images
1 containing phrase/(target name)

_________________
1. Cell of the cropped images ({3d matrix1},{3d matrix2},...)
2. Target name (a cell string {'person_nextto_car'}, {'person'}, {'car'})

________

Errors:
In ErrorImgs.mat, 16 "not a jpeg file" errors and 1 blank.

For jpeg file errors, try to convert to a different format and perform crop?


